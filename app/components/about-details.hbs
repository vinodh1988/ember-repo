 {{#if firstpage}}
   <div class ="page">
       <p>page1 </p>
       <button  {{on "click" this.performForward}}  class="btn btn-primary"> Continue Reading...!!!</button>
   </div>

 {{/if}}

 {{#if secondpage}}
  <div class ="page">
       <p>page 2 </p>
      <button   {{on "click" this.performForward}} class="btn btn-primary"> Continue Reading...!!!</button>
      <button   {{on "click" this.performBackward}}  class="btn btn-primary"> Go back</button>
    </div>
 {{/if}}

{{#if thirdpage}}
    <div class ="page">
      <p>page 3 </p>
    <button   {{on "click" this.performBackward}} class="btn btn-primary"> Go back</button>
    </div>
 {{/if}}